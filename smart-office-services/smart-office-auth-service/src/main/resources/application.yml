server:
  port: 9101

spring:
  profiles:
    active: local
  application:
    name: smart-office-auth-service

smart-office:
  auth:
    token:
      enabled: ${AUTH_TOKEN_REDIS_ENABLED:true}
      key-prefix: ${AUTH_TOKEN_KEY_PREFIX:smart-office:auth:token}
